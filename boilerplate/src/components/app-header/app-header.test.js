//example test
define(['components/app-header'],function (appHeader) {
	describe('app-header', function () {
		describe('the `...` method on app-header',function () {
			it('is everything you expect it to be', function () {
				expect('add test here').toBeDefined();
			});
		});
	});
});